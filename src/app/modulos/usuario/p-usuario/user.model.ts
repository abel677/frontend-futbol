export class User{
    // img:string;
    // name:string;
    // email:string;
    // password:string;
    // rol_id:number;
    constructor
    (
       public id:number, public image:string, public name:string, public email:string, public password:string, public rol_id:number
    ){

    }
}